# MUD NAS Progress

更新日期: 2026-03-22

## 当前结论

项目已经在威联通 NAS 上成功跑通，基础登录、建号、进房间、管理员角色创建都正常。

当前形态不是对 `https://jz.faith.wang/` 的原样迁移，而是基于 Evennia 搭出的可自控中文 MUD 骨架。

## NAS 运行信息

- NAS 地址: `192.168.2.222`
- 部署目录: `/share/CACHEDEV1_DATA/Container/mud-nas-evennia/`
- 容器名: `jiuzhou-like-mud`
- Docker 路径: `/share/CACHEDEV1_DATA/.qpkg/container-station/bin/docker`
- GitHub 仓库: `https://github.com/xiaocai218/mud-nas-evennia.git`
- 对外端口:
  - `4000` Telnet
  - `4001` Web
  - `4002` WebSocket

## 当前可用访问方式

- Web: `http://192.168.2.222:4001`
- Telnet: `192.168.2.222:4000`

## 当前管理员账号

- 用户名: `admin`
- 密码: `MudAdmin_2026!`

建议后续登录后尽快修改。

## 已完成事项

### 1. Docker 部署跑通

- 修复了 `docker-compose.yml` 的启动命令引号问题
- 修复了 QNAP Container Station 下的实际 Docker 路径问题
- 完成了容器创建、重建、日志验证
- 确认 Evennia `Portal` 和 `Server` 都已成功运行

### 2. Evennia 项目初始化完成

- 创建了 `game/mygame` 项目目录
- 执行了数据库迁移
- 创建了管理员账号
- 完成了首次世界初始化

### 3. 第一轮中文化已完成

- 登录欢迎页改成中文
- 默认出生房间从 `Limbo` 改为 `青云渡`
- 出生点描述改成中文文案
- 为后续新建房间补了默认中文描述逻辑

### 4. 第一批可玩内容已完成

- 新增中文命令 `新手`
- 新增中文命令 `状态`
- 新增中文命令 `修炼`
- 新增中文帮助条目
- 搭建了最小新手区域:
  - `青云渡`
  - `问道石阶`
  - `古松林`
- 打通了基础移动出口:
  - `北`
  - `南`
  - `东`
  - `西`
- 加入了最小修炼循环:
  - 消耗体力
  - 获取修为
  - 自动提升炼气小境界

### 5. 第二批基础玩法已完成

- 新增 `休息` 命令，用于恢复体力
- 新增 `练拳` 命令，用于在木人桩前练习
- 在 `古松林` 放置可交互对象 `木人桩`
- 形成了最小闭环:
  - `修炼`
  - `休息`
  - `练拳`
  - `状态`

### 6. 第三批交互玩法已完成

- 新增 `交谈` 命令
- 新增 `攻击` 命令
- 在 `青云渡` 放置新手 NPC `守渡老人`
- 在 `古松林` 放置训练目标 `青木傀儡`
- 打通了最简战斗测试流程:
  - 找到目标
  - 消耗体力攻击
  - 目标掉血
  - 击败后获得修为
  - 训练目标自动复位

### 7. 第四批物品雏形已完成

- 新增 `背包` 命令
- 击败 `青木傀儡` 后会掉落 `青木碎片`
- 物品会直接进入角色背包
- `状态` 命令已显示背包物品数量
- 新增基础物品类型 `typeclasses.items.Item`

### 8. 第五批资源循环已完成

- 新增 `炼化` 命令
- `青木碎片` 可被炼化为修为
- 掉落物第一次真正接入了成长循环
- 当前已经形成:
  - 战斗获取掉落
  - 背包查看掉落
  - 炼化掉落提升修为

### 9. 第六批战斗反馈已完成

- 新增 `调息` 命令
- `青木傀儡` 受到攻击后会固定反击
- 玩家现在会在战斗中损失气血
- 气血与体力正式分成两条资源线
- 气血归零后会自动退回 `青云渡` 并重置基础状态

### 10. 第七批任务雏形已完成

- 新增 `任务` 命令
- `守渡老人` 现在会正式发放新手任务
- 新手任务目标: 击败一次 `青木傀儡`
- 回去再次 `交谈 守渡老人` 可交付任务
- 任务奖励:
  - 修为
  - `渡口药包`

### 11. 结构收口已完成

- 将原先集中在单文件中的命令拆分到多个模块
- 新增 `systems/` 目录承载可复用规则逻辑
- 新增架构说明文档 `docs/architecture.md`
- 当前重构目标是:
  - 功能表现不变
  - 后续扩展更稳
  - 降低继续堆功能时的耦合

### 12. 可用物品雏形已完成

- 新增 `使用` 命令
- `渡口药包` 现在可以直接使用
- 使用后会恢复气血并消耗物品
- 任务奖励第一次真正接入了恢复循环

### 13. 第二阶段任务已完成

- `守渡老人` 的新手任务现在扩成两段
- 第一阶段仍然是击败 `青木傀儡`
- 第二阶段新增目标 `山石傀儡`
- 第二阶段目标位于 `问道石阶`
- `任务` 命令现在会显示当前阶段目标
- 第二阶段完成后会获得：
  - 修为
  - `石阶护符`

### 14. 第二类敌人与掉落已完成

- 在 `问道石阶` 新增 `山石傀儡`
- 第二类敌人已接入通用战斗规则
- `山石傀儡` 会掉落 `山纹石屑`
- 战斗系统开始从单目标脚本转向按对象属性驱动

### 15. 第二类可用奖励已完成

- `石阶护符` 现在可以直接使用
- 使用后会恢复体力并消耗物品
- 奖励物第一次同时覆盖：
  - 气血恢复
  - 体力恢复

### 16. 第三阶段任务已完成

- 新增第二个任务 NPC `巡山弟子`
- `守渡老人` 现在会在第二阶段完成后引导玩家前往新区域
- 第三阶段任务目标: 击败一次 `雾行山魈`
- 第三阶段交付人变为 `巡山弟子`
- 当前新手引导已经形成三段式流程

### 17. 新区域与第三类掉落已完成

- 新增区域 `溪谷栈道`
- `问道石阶` 现在可以继续向北前进
- 新增敌人 `雾行山魈`
- 新增掉落 `雾露果`
- `雾露果` 不走炼化路线，而是直接用于恢复

### 18. 任务结构继续收口

- `systems/quests.py` 现在开始承载任务阶段元数据
- `任务` 命令的展示文本已收敛到任务系统层生成
- 旧版任务存档兼容逻辑仍然保留
- 当前目标是继续减少 `commands/social.py` 中的手写状态分支

### 19. 奖励配置化与第一条支线已完成

- 主线三段任务的修为奖励和道具奖励已开始收进 `systems/quests.py`
- 新增第一条轻量支线 `雾露代药`
- 新增支线 NPC `药庐学徒`
- 支线目标: 交付一个 `雾露果`
- 支线奖励: `回春散`

### 20. 任务定义已独立到 JSON

- 新增任务配置文件 `game/mygame/world/data/quests.json`
- 主线阶段、支线、奖励、状态文案、战斗进度映射已从 Python 常量迁移到 JSON
- `systems/quests.py` 现在主要负责读取配置和执行流程
- 后续增删任务时，不再需要优先改代码常量

### 21. 怪物与物品定义已独立到 JSON

- 新增 `game/mygame/world/data/enemies.json`
- 新增 `game/mygame/world/data/items.json`
- 怪物参数、掉落和任务标记开始从代码迁移到配置文件
- 物品说明、炼化收益、使用效果开始从代码迁移到配置文件
- `systems/items.py` 与 `systems/combat.py` 进一步转为“读取配置并执行”

### 22. 房间与 NPC 定义已独立到 JSON

- 新增 `game/mygame/world/data/rooms.json`
- 新增 `game/mygame/world/data/npcs.json`
- 房间描述、出口关系开始从代码迁移到配置文件
- NPC 与常驻对象的基础定义开始从代码迁移到配置文件
- `world/start_area.py` 现在主要负责读取配置并落地世界对象

### 23. NPC 对话文案已开始独立到 JSON

- 新增 `game/mygame/world/data/dialogues.json`
- `social.py` 中的基础 NPC 对话与提醒文案开始迁移到配置文件
- 新增 `systems/dialogues.py` 负责读取对话配置
- 当前逻辑判断仍在代码里，但文案维护已明显变轻

### 24. NPC 交谈路由已开始独立到 JSON

- 新增 `game/mygame/world/data/npc_routes.json`
- NPC 身上的 `talk_route` 现在会指向配置化路由
- `social.py` 已从“按 NPC 名称硬编码分支”开始转向“按路由处理器分发”
- 当前任务判定仍在代码里，但 NPC 接线层已经更容易扩展

### 25. 主线阶段推进已进一步独立到 JSON

- `game/mygame/world/data/quests.json` 现在除了任务标题和奖励外，也开始承载阶段完成后的流转状态
- 主线阶段开始时需要重置的进度字段，已经跟随阶段定义一起配置
- 旧版 `completed` 存档到新多阶段任务流的兼容映射，也已收进任务配置
- `systems/quests.py` 进一步转成“读取任务配置并执行”，继续减轻手写阶段函数的负担

### 26. NPC 交谈条件已进一步独立到 JSON

- `game/mygame/world/data/npc_routes.json` 现在除了路由名，也开始承载触发条件和处理步骤
- `systems/npc_routes.py` 现在会按配置执行对话、接任务、交任务和奖励提示
- `commands/social.py` 已进一步收薄，主要只保留交谈入口和目标校验

### 27. WebClient 公网依赖已本地化

- 新增 `game/mygame/web/templates/webclient/base.html` 覆盖 Evennia 默认 WebClient 模板
- 新增 `game/mygame/web/static/webclient/vendor/`，本地托管 jQuery、Bootstrap、Popper、Golden Layout、Favico
- `server/conf/settings.py` 已显式接入项目 `web/templates` 与 `web/static`
- 目标是解决公网首开时因外部 CDN 不稳定导致的红字提示和偶发前端加载失败

### 28. H5 前端接入评估已留档

- 新增 `docs/frontend_h5_plan.md`
- 当前结论：
  - 后端应保持前端框架中立
  - H5 第一版默认推荐 `Vue 3 + TypeScript + Pinia`
  - 不论最终选 Vue 还是 React，后端都要先补协议层和工具层
- 已明确后续预留方向：
  - `serializers`
  - `action_router`
  - `event_bus`
  - `client_protocol`

### 29. H5 协议基础设施第一版已落地

- 新增 `systems/serializers.py`
  - 输出角色、房间、区域、地图层级、背包、任务状态的结构化 DTO
- 新增 `systems/action_router.py`
  - 提供结构化 action 到现有系统逻辑的分发入口
- 新增 `systems/event_bus.py`
  - 统一事件信封格式
- 新增 `systems/client_protocol.py`
  - 统一 action / response 消息包装和基础校验
- 当前已接入的结构化动作：
  - `bootstrap`
  - `look`
  - `move`
  - `read`
  - `gather`
  - `trigger_object`
  - `use_item`
  - `buy_item`

### 30. 本地 Evennia 测试环境已准备

- 在 `C:\Users\CZH\Documents\Playground` 下新增本地包目录：
  - `evennia-local-env`
- 已安装：
  - `evennia==6.0.0`
  - 对应 Django / Twisted / DRF 依赖
- 当前通过导入校验：
  - `Evennia 6.0.0`
  - `Django 6.0.3`
  - `Twisted 24.11.0`
- 新增本地启动脚本：
  - `run-evennia-local.ps1`
- 新增说明文档：
  - `docs/local_evennia_env.md`
- 当前没有直接使用 `venv`，原因是本机 `Python 3.14` 的 `ensurepip` 阶段存在权限错误，改用 `wheel 下载 + 本地解压` 方案落地

### 31. H5 协议层本地测试已补齐

- 新增测试文件：
  - `tests/test_h5_protocol.py`
- 新增测试脚本：
  - `C:\Users\CZH\Documents\Playground\run-h5-local-tests.ps1`
- 当前已覆盖：
  - `client_protocol`
  - `serializers`
  - `action_router`
- 当前测试目标是：
  - 不依赖完整 Evennia 运行态
  - 优先回归 H5 接口层的结构化返回和动作分发

### 32. `talk / attack` 已接入 H5 结构化动作层

- `systems/action_router.py` 已新增：
  - `talk`
  - `attack`
- 当前 `talk` 会复用既有 NPC 路由，并返回消息列表与任务文本
- 当前 `attack` 会复用既有战斗系统，并返回战斗结果、角色状态和背包快照
- 本地测试已新增对应覆盖

### 33. `quest / shop` DTO 与 H5 协议草案已细化

- `systems/serializers.py` 已新增：
  - `serialize_shop`
  - `serialize_shop_by_id`
  - `serialize_quest_log`
- `build_bootstrap_payload` 现在返回更完整的 quest log 结构
- 新增协议文档：
  - `docs/h5_api_protocol_v1.md`
- 当前文档已明确：
  - HTTP 草案
  - WebSocket 草案
  - action 列表
  - CharacterDTO / RoomDTO / QuestLogDTO / ShopDTO 草案
- NPC 交谈逻辑开始从“手写分支”转向“配置驱动的步骤解释器”

### 27. 怪物摆放关系已进一步独立到 JSON

- `game/mygame/world/data/enemies.json` 现在除了怪物属性，也开始承载怪物所在房间
- `world/start_area.py` 已不再手写 `青木傀儡`、`山石傀儡`、`雾行山魈` 的房间映射
- 新增怪物时，除了属性和掉落外，也可以直接在敌人配置里指定落点

### 28. 房间交互对象已进一步独立到 JSON

- `game/mygame/world/data/objects.json` 已独立承载房间内的常驻交互对象模板
- `木人桩` 已从 `npcs.json` 中拆出，避免 NPC 与交互物混放
- `world/start_area.py` 现在分别读取 NPC、交互对象、敌人三类配置，后续扩展更清晰

### 29. 交互对象模板已开始支持对象类型

- `game/mygame/world/data/objects.json` 现在开始承载 `object_type`
- 新增了最小可读对象 `渡口告示牌`
- 新增命令 `阅读 <目标>`，用于查看公告牌这类对象内容
- `systems/world_objects.py` 已作为对象交互能力的最小规则层落地

### 30. 采集点已接入对象系统

- 新增采集点 `松纹草丛`
- 新增命令 `采集 <目标>`
- `松纹草` 现在可以通过采集获得并直接进入背包
- 对象系统已从“可读对象”扩展到“可采集对象”

### 31. 传送点已接入对象系统

- 新增传送对象 `回渡石`
- 新增命令 `触发 <目标>`
- `回渡石` 现在可用于从 `溪谷栈道` 快速返回 `青云渡`
- 对象系统已进一步扩展到传送类交互

### 32. 任务碑已接入对象系统

- 新增任务碑对象 `问道路碑`
- `阅读 问道路碑` 现在可直接显示当前主线/支线任务引导
- 对象系统开始支持“静态说明 + 动态任务状态”的混合提示

### 33. 临时增益点已接入对象系统

- 新增增益点对象 `凝神蒲团`
- `触发 凝神蒲团` 现在可获得一段时间的修炼加持
- `状态` 命令已开始显示当前生效中的临时加持
- 修炼系统已能读取对象授予的临时增益

### 34. 门派入口对象已接入对象系统

- 新增门派入口对象 `青云山门`
- 新增可进入区域 `青云外门前坪`
- `触发 青云山门` 会按主线完成状态决定是否放行
- 对象系统开始支持“带解锁条件的区域入口”

### 35. 稳定内容 ID 已开始补齐

- 房间、对象、NPC、怪物、物品、任务开始补充稳定 `id/content_id`
- 系统层开始优先兼容按这些稳定标识进行引用
- 目标是逐步减少中文显示名直接参与系统逻辑的情况

### 36. 多支线支持与第二条支线已完成

- `systems/quests.py` 已支持按任务分别读取支线状态
- `任务` 面板现在可以同时展示多条已接取支线
- `systems/npc_routes.py` 已新增按指定支线判断状态的条件匹配
- 新增区域 `听泉药圃`
- 新增 NPC `药圃执事`
- 新增采集点 `露华药畦`
- 新增第二条支线 `药圃添香`
- 新增物品 `露华草`
- 新增支线奖励 `清心香囊`

### 37. Area 设计基线已补充文档

- 新增 `docs/area_design.md`
- 明确后续地图结构从“只有 room”升级为“area + room”两层
- 明确 `area_exit` 作为区域到区域的迁移结构
- 明确 `青云渡新手村` 将作为第一个标准区域
- 明确后续基础设施包括：
  - 小药铺
  - 铁匠铺
  - 村舍
  - 杂货摊
  - 区域出口
  - 最小商店

### 38. Area 结构第一阶段已落地

- 新增 `game/mygame/world/data/areas.json`
- 新增 `game/mygame/world/data/area_exits.json`
- `rooms.json` 已开始补 `area_id`
- 当前已正式划分两个区域：
  - `青云渡新手村`
  - `青云外门`
- `状态` 已开始显示当前所属区域
- 新增 `systems/areas.py` 统一读取区域与区域出口信息

### 39. 登录链与角色命令集已重新拆分

- 登录相关命令集保持 Evennia 官方默认：
  - `AccountCmdSet`
  - `UnloggedinCmdSet`
  - `SessionCmdSet`
- 角色进入游戏后的命令集恢复为项目自定义 `commands.default_cmdsets.CharacterCmdSet`
- 当前策略是：
  - 登录链尽量不自定义，优先稳定
  - 游戏内中文命令和玩法命令只挂在 `CharacterCmdSet`
- 后续如果再出现登录异常，优先检查 `settings.py` 中四条 `CMDSET_*` 配置是否被误改

### 40. 世界分层已升级到 map / zone / area / room 设计

- 新增 `game/mygame/world/data/maps.json`
- 新增 `game/mygame/world/data/zones.json`
- `areas.json` 已开始补 `zone_id`
- 当前第一版世界结构已固定为：
  - `神洲大陆`
  - `宁洲`
  - `青云渡新手村`
- `青云外门`
- 新增世界结构文档 `docs/world_structure.md`
- 当前这一步先完成设计与配置落点，后续再把 `状态`、导航、区域迁移和商店系统逐步接到这套分层上

### 41. 项目命令基类已切回 Evennia 标准 MuxCommand

- `game/mygame/commands/command.py` 已从基础 `Command` 切回 `MuxCommand`
- 当前目的不是改玩法，而是统一命令解析行为
- 这能避免中文命令和带参数命令在不同客户端下出现匹配不稳定的情况
- 后续新增项目命令继续统一继承这一层，不再直接继承更底层的 Evennia 基础命令类

### 42. 角色类型类已显式绑定项目 CharacterCmdSet

- `game/mygame/typeclasses/characters.py` 现在显式声明 `cmdset_character = "commands.default_cmdsets.CharacterCmdSet"`
- 当前目的不是新增功能，而是避免角色命令集只依赖 settings 间接推导
- 这能减少已有角色、热重载和新建角色之间出现命令集不一致的概率

### 43. 新手村基础设施房间与最小商店已落地

- `rooms.json` 已新增：
  - `村口杂货摊`
  - `小药铺`
  - `铁匠铺`
  - `临溪村舍`
- `areas.json` 已将这些设施房间正式归入 `青云渡新手村`
- `npcs.json` 已新增商店 NPC：
  - `杂货摊掌柜`
  - `药铺掌柜`
- 新增 `world/data/shops.json`
- 新增命令：
  - `商店`
  - `购买`
- `character_defaults.json` 已开始补基础货币 `铜钱`
- `状态` 已开始显示当前铜钱
- 当前最小商店阶段只支持购买，不支持售回

### 44. 命令不可执行问题已定位并补入维护档案

- 现象是：
  - 登录正常
  - 移动正常
  - 项目命令出现在命令面板里
  - 但输入 `状态 / 新手 / 任务 / 攻击` 会提示不可用
- 根因不是玩法逻辑，而是 NAS 容器里的 live 文件仍然是旧版
- 这次确认的关键点是：
  - 不能只看本地仓库
  - 不能只看 `evennia reload` 返回成功
  - 必须直接查看容器中的 `/usr/src/game/...` live 文件
- 已把完整排查路线写入 `docs/maintenance.md`
- 后续若再遇到类似问题，优先按“查看运行时 settings -> 查看 live 文件 -> docker cp 覆盖 -> reload”这条线处理

### 45. H5 HTTP 路由骨架已落地

- 新增 `game/mygame/web/urls.py`
- 新增 `game/mygame/web/api/urls.py`
- 新增 `game/mygame/web/api/views.py`
- 当前已落地：
  - `GET /api/h5/`
  - `GET /api/h5/bootstrap/`
  - `GET /api/h5/quests/`
  - `GET /api/h5/shops/<shop_id>/`
  - `POST /api/h5/action/`
  - `GET /api/h5/ws-meta/`
- 当前鉴权依赖：
  - Django Session
  - Evennia 当前活跃角色
- 新增测试：
  - `tests/test_h5_http_routes.py`
- 当前本地 H5 相关测试总计 20 个用例通过

### 46. H5 视觉参考规范已留档

- 基于用户提供的九洲类参考图，新增：
  - `docs/frontend_ui_style_guide.md`
- 当前已明确 H5 第一版视觉方向：
  - 白底留白
  - 黑白灰主色
  - 蓝色高亮
  - 顶部资源栏
  - 底部五栏导航
  - 中部内容卡片化
  - 详情优先 modal 化
- 后续地图页、房间页、人物详情、设置页、系统页默认按这份规范实现

### 47. H5 前端骨架已落地并通过构建

- 新增目录：
  - `frontend/h5`
- 技术栈：
  - `Vue 3`
  - `Vite`
  - `TypeScript`
- 当前已落地：
  - 顶部资源栏
  - 底部五栏导航
  - 地图页
  - 房间页
  - 更多页
  - NPC / 玩家详情弹窗
  - 世界入口弹窗
- 当前说明：
  - 仍使用 mock 数据
  - 还未进入完整接口联调
- 当前验证：
  - 已执行 `npm install`
  - 已执行 `npm run build`
  - 构建通过

### 48. H5 账号接口已落地

- 新增接口：
  - `POST /api/h5/auth/login/`
  - `POST /api/h5/auth/logout/`
  - `GET /api/h5/account/characters/`
  - `POST /api/h5/account/characters/select/`
- 当前实现方式：
  - 复用 Django Session 登录态
  - 角色激活通过 `request.session["puppet"]`
  - 与 Evennia 网站层现有思路保持一致
- 新增 DTO 能力：
  - `serialize_account`
  - `serialize_character_summary`
- 当前本地 H5 测试总数已扩展到 25 个
- 测试全部通过

### 49. H5 前端已接入第一批真实账号链路

- `frontend/h5` 已新增：
  - `src/services/api.ts`
  - `src/services/gameClient.ts`
  - `src/components/LoginPanel.vue`
  - `src/components/CharacterSelectModal.vue`
- 当前前端已支持：
  - 登录
  - 角色列表
  - 角色选择
  - bootstrap 拉取
- 当前策略：
  - 接口可达时走真实 `/api/h5/*`
  - 接口不可达时退回 mock 页面骨架
- 当前本地验证：
  - `npm run build` 通过

### 50. 前端视觉规范已补齐 PC 端参考基线

- 用户已补充 PC 端九洲类参考图
- `docs/frontend_ui_style_guide.md` 已从单一手机规范升级为：
  - 手机端浅色轻客户端
  - PC 端深色工作台式布局
- 当前 PC 端设计结论已固定：
  - 左中右三栏主界面
  - 右栏系统矩阵
  - 中部地图 / 房间 / 聊天主体区
  - 大尺寸 modal 承担背包、排行、组队、设置、世界地图、任务对话
- 后续如果继续做 PC 浏览器页，不再直接照搬手机端页面结构

### 51. H5 实时传输骨架已补第一层 fallback

- 新增接口：
  - `GET /api/h5/events/poll/`
- `GET /api/h5/ws-meta/` 现在会返回：
  - WebSocket 保留端点
  - poll fallback 端点
  - poll interval
  - 支持事件列表
  - 当前登录态与活跃角色信息
- 新增 `systems.event_bus.build_event_batch`
- 前端已新增 `src/services/realtimeClient.ts`
- 当前策略不是直接伪造实时玩法，而是先固定：
  - 实时事件批格式
  - cursor 语义
  - 前端 realtime client 壳
  - WebSocket / polling 共用的事件模型

### 52. 实时聊天频道基线已落地

- 新增命令：
  - `世界 <内容>`
  - `队伍 <内容>`
  - `私聊 <玩家> <内容>`
  - `频道`
  - `静音 <频道>`
  - `取消静音 <频道>`
- 新增 `systems/chat.py`
  - 当前复用 Evennia channel 能力
  - 统一处理世界频道、队伍频道占位、私聊、系统消息和频道静音
- 当前第一版频道范围：
  - `世界`
  - `队伍`
  - `私聊`
  - `系统`
- 当前明确不做：
  - 历史消息留档
  - 离线邮箱
  - 黑名单/禁言
  - 跨区频道权限
- H5 协议已同步预留：
  - `chat_world`
  - `chat_team`
  - `chat_private`
  - `chat.message`
- `event_bus.py` 已新增最小账号事件队列，`events/poll` 可返回聊天事件
- 本地回归已新增聊天系统测试与 H5 接口测试，当前总计 40 个用例通过

### 53. 系统消息已接入任务/购买/掉落提示

- `systems/chat.py` 已新增 `notify_player`
- `systems/npc_routes.py` 当前会在主线/支线接取与完成时发送 `[系统]` 提示
- `systems/shops.py` 当前会在购买成功后发送 `[系统]` 提示
- `systems/combat.py` 当前会在击杀并掉落时发送 `[系统]` 提示
- 这层仍然只做实时，不做历史存档
- 新增回归测试：
  - `tests/test_system_notices.py`
- 当前本地测试总数已扩展到 `43`，全部通过

## 已修改文件

- `docker-compose.yml`
- `README.md`
- `game/mygame/server/conf/connection_screens.py`
- `game/mygame/server/conf/at_initial_setup.py`
- `game/mygame/server/conf/settings.py`
- `game/mygame/commands/command.py`
- `game/mygame/commands/core.py`
- `game/mygame/commands/cultivation.py`
- `game/mygame/commands/combat.py`
- `game/mygame/commands/social.py`
- `game/mygame/commands/inventory.py`
- `game/mygame/commands/default_cmdsets.py`
- `game/mygame/systems/realms.py`
- `game/mygame/systems/player_stats.py`
- `game/mygame/systems/world_objects.py`
- `game/mygame/systems/items.py`
- `game/mygame/systems/quests.py`
- `game/mygame/systems/npc_routes.py`
- `game/mygame/systems/combat.py`
- `game/mygame/world/help_entries.py`
- `game/mygame/world/start_area.py`
- `game/mygame/world/data/enemies.json`
- `game/mygame/world/data/npc_routes.json`
- `game/mygame/world/data/objects.json`
- `game/mygame/world/data/rooms.json`
- `game/mygame/world/data/areas.json`
- `game/mygame/world/data/area_exits.json`
- `game/mygame/world/data/npcs.json`
- `game/mygame/world/data/items.json`
- `game/mygame/world/data/dialogues.json`
- `game/mygame/world/data/quests.json`
- `game/mygame/world/data/effects.json`
- `game/mygame/typeclasses/characters.py`
- `game/mygame/typeclasses/rooms.py`
- `docs/architecture.md`
- `docs/area_design.md`
- `docs/h5_api_protocol_v1.md`
- `game/mygame/web/urls.py`
- `game/mygame/web/api/urls.py`
- `game/mygame/web/api/views.py`

## 当前目录结构

```text
mud-nas-evennia/
  docker-compose.yml
  .env.example
  README.md
  docs/
  game/
    mygame/
      server/conf/
      typeclasses/
```

## 当前行为说明

### 登录前

用户连接时会看到中文欢迎页，提示使用：

- `connect <账号> <密码>`
- `create <账号> <密码>`

### 登录后

玩家会进入中文出生房间 `青云渡`。

### 当前可直接试玩的命令

- `新手`
- `状态`
- `频道`
- `世界 <内容>`
- `私聊 <玩家> <内容>`
- `静音 <频道>`
- `取消静音 <频道>`
- `组队`
- `建队 [队伍名]`
- `邀请 <玩家名>`
- `接受邀请 [队长名]`
- `离队`
- `修炼`
- `休息`
- `练拳`
- `交谈 守渡老人`
- `攻击 青木傀儡`
- `攻击 山石傀儡`
- `攻击 雾行山魈`
- `背包`
- `炼化 青木碎片`
- `炼化 山纹石屑`
- `调息`
- `任务`
- `使用 渡口药包`
- `使用 石阶护符`
- `使用 雾露果`
- `look`
- `北` / `南` / `东` / `西`
- `help 新手`
- `help 修炼`
- `help 休息`
- `help 练拳`

## 2026-03-24 实时聊天收口与最小组队骨架

本轮先把多人存在感的底层打通，优先完成实时聊天基线。

新增的实时聊天入口：

- `世界`
- `队伍`
- `私聊`
- `系统`
- `频道`
- `静音`
- `取消静音`

当前约束：

- 只做实时，不做历史消息留档
- `系统` 频道只读，玩家不能把它当普通聊天频道使用
- `队伍` 频道已经不再只是占位，而是会检查真实队伍状态

同时补了最小组队系统：

- `组队`
- `建队 [队伍名]`
- `邀请 <玩家名>`
- `接受邀请 [队长名]`
- `离队`

当前组队规则：

- 玩家可以独立建队
- 只有队长可以邀请其他玩家
- 被邀请玩家可以接受邀请入队
- 队长离队时会自动把队长转给下一位成员
- 最后一名成员离队时，队伍会自动解散

这轮也把系统通知接进了实际业务路径：

- 任务推进
- 商店购买
- 战斗击杀 / 掉落 / 修为提示

本地校对结果：

- `compileall` 通过
- 本地自动化测试 `47` 项通过

## 2026-03-24 聊天频道兼容层修复

为兼容 fresh deploy 和旧数据库里的频道对象，本轮补了聊天频道兼容层：

- 频道内部键改为：
  - `chat_world`
  - `chat_team`
  - `chat_system`
- 启服时自动执行 `ensure_all_channels()`
- 自定义命令显式接管：
  - `世界`
  - `队伍`
  - `私聊`
  - `频道`
  - `静音`
  - `取消静音`
- 新增只读拦截命令：
  - `系统`
- 启服时会自动清理旧账户上的频道 nick/alias，避免 `世界 <内容>` 在命令解析前被 Evennia 旧频道别名改写

这样可以避免 Evennia 底层动态频道命令抢占 `世界/系统`，并保证系统频道只能由业务逻辑推送。

## 2026-03-24 组队邀请补全

本轮继续把最小组队骨架补到更稳的状态：

- 新增：
  - `拒绝邀请 [队长名]`
- 组队邀请现在带有效期
- 过期邀请会自动清理，并在接受时返回明确提示
- `组队` 查看会显示当前待处理邀请数量
- 新增 `邀请列表`，可直接查看有效邀请来源、队伍名和大致剩余时间

这轮上线后，NAS 重点回归项应包括：

1. `系统 1` 必须被拒绝
2. 两个玩家的建队 / 邀请 / 接受邀请 / 队伍频道聊天
3. 队长离队后的自动转移

## 2026-03-24 运行时持久化目录调整

为避免重新 clone 或替换部署目录时把数据库和私有配置一起丢掉，部署结构新增了独立的 `runtime/` 持久化目录。

当前约定：

- `runtime/conf/secret_settings.py`
- `runtime/evennia.db3`
- `runtime/logs/`
- `runtime/static/`

`docker-compose.yml` 启动时会自动把这些运行时文件和目录挂接回 `server/` 下。

这样后续即使更新代码目录，也不需要再把数据库和私有配置继续放在 `game/mygame/server/` 里。

## 已知现状

- 当前仍然大量沿用 Evennia 默认命令体系
- 已有基础修为和境界骨架，但还不是完整修仙系统
- 已经有最简战斗测试，但还不是正式战斗系统
- 已经有最小背包和掉落，但还不是完整物品系统
- 已经有基础炼化，但还不是完整资源使用体系
- 已经有基础战斗反馈，但还不是正式回合战斗
- 当前已有两段新手任务，但还不是完整任务链
- 当前已有三段新手任务，主线阶段流转已开始配置化
- 当前 NPC 交谈条件和处理步骤也已开始配置化
- 当前怪物摆放关系也已开始配置化
- 当前房间内交互对象也已开始配置化
- 当前交互对象类型和最小非战斗交互也已开始配置化
- 当前采集点也已正式接进资源循环
- 当前传送点也已正式接进对象系统
- 当前任务碑也已正式接进对象系统
- 当前临时增益点也已正式接进对象系统
- 当前恢复点对象也已正式接进对象系统
- 当前带解锁条件的区域入口也已正式接进对象系统
- 当前稳定内容 ID 也已开始进入地基层
- 当前对象交互也已开始统一收口到配置字段
- 当前境界阈值已开始配置化
- 当前 buff / debuff 效果结构已开始配置化
- 当前物品与对象效果已开始收口到统一执行器
- 当前 NPC 路由动作也已开始收口到处理器注册表
- 当前支线任务执行也已开始收口到通用状态字段
- 当前玩家默认属性也已开始收口到角色模板配置
- 当前帮助文案与新手指引也已开始收口到帮助内容配置
- 当前多个 JSON 内容读取也已开始收口到统一加载器
- 当前管理员内容维护入口也已开始落地
- 当前内容一致性校验也已开始落地
- 当前已开始支持多条支线并存，不再默认只围绕一条支线运转
- 当前地图结构也已开始从“只有 room”升级为“area + room”
- 已经有基础可用物品，但还不是完整道具体系
- 还没有门派、挂机等玩法
- 还没有做 Web 界面的定制化

## 建议下一步

优先建议按这个顺序继续：

1. 把更多 NPC 交谈触发条件和任务入口继续整理进配置层
2. 给对象模板继续增加一次性事件点、区域机关、特殊试炼点等类型
3. 把战斗扩成更正式的敌人差异和伤害节奏
4. 做恢复类资源以外的属性体系
5. 做门派和功法雏形

## 备注

本次远端操作约束如下，后续维护应尽量保持一致：

- 只使用 Docker 相关命令
- 只在 `/share/CACHEDEV1_DATA/Container/mud-nas-evennia/` 下读写
- 不改 NAS 宿主机其他配置
